// Text
export const text = {
	menu: "Menu",
	toggle: "Toggle",
};

// Icons
export const icons = {
	menu: "mdi-menu",
};

// Tooltips
export const tooltips = {
	menu: `${text.toggle} ${text.menu.toLowerCase()}`,
};
